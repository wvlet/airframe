name: Documentation

on: [push]

jobs:
  doc:
    name: Update docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-ruby@v1
        with:
          ruby-version: '2.x'
      - name: Install JekyllUpdate docs
        run: |
          gem update --system
          gem install sass
          gem install jekyll -v 3.8.5
      - name: Publish doc
        run: ./sbt docs/publishMicrosite
