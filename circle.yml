machine:
  timezone: UTC
  java:
    version: oraclejdk8

dependencies:
  cache_directories:
    - "~/.sbt"
    - "~/.ivy2"
  override:
    - ./sbt "+ compile"

test:
  override:
    - ./sbt ++2.11.8 test
    - ./sbt coverage test coverageReport
  post:
    - ./sbt coverageAggregate coveralls
